---
# Tasks for Redhat BIND DNS install
- name: 'Install the lastest ISC Bind DNS Server'
  ansible.builtin.package:
    name: "{{ package_name }}"
    state: latest

# Using user 'wheel' for RedHat based distros Rocky.
#- name: 'BIND config file'
#  ansible.builtin.template:
#    src: 'named.conf.j2'
#    dest: "/etc/dhcp/dhcpd.conf"
#    owner: 'root'
#    group: "{{ admin_group }}"
#  register: dhcpd_conf

#- name: Start service {{ service_name }}, if not started
#  ansible.builtin.service:
#    name: "{{ service_name }}"
#    state: started
#    enabled: yes
#  when: dhcpd_conf.changed