#SPDX-License-Identifier: MIT-0
---
# tasks file for roles/slurm
# We expect the deb_repos role to have already been run at this point

- name: 'Install slurmdbd DEB file from Katello'
  ansible.builtin.apt:
    name: slurm-smd-slurmdbd
    state: present
    update_cache: true
